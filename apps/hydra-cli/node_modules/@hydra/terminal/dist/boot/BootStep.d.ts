/**
 * HYDRA I.OS
 * Boot Step
 *
 * Represents a single step in the Hydra
 * startup sequence.
 */
export interface BootStep {
    /**
     * Human-readable boot message.
     */
    readonly message: string;
    /**
     * Boot status.
     */
    readonly status: "OK";
}
//# sourceMappingURL=BootStep.d.ts.map