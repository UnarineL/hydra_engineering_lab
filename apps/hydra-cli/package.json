{
  "name": "hydra-cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "pnpm build && node ./dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hydra/kernel": "workspace:*",
    "@hydra/terminal": "workspace:*"
  }
}